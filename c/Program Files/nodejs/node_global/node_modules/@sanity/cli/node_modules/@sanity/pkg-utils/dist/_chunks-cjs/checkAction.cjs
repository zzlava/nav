"use strict";
var check = require("./check.cjs"), handleError = require("./handleError.cjs");
async function checkAction(options) {
  try {
    await check.check({
      cwd: process.cwd(),
      strict: options.strict,
      tsconfig: options.tsconfig
    });
  } catch (err) {
    handleError.handleError(err);
  }
}
exports.checkAction = checkAction;
//# sourceMappingURL=checkAction.cjs.map
