"use strict";
var init = require("./init.cjs"), handleError = require("./handleError.cjs");
async function initAction(options) {
  try {
    await init.init({
      cwd: process.cwd(),
      path: options.path
    });
  } catch (err) {
    handleError.handleError(err);
  }
}
exports.initAction = initAction;
//# sourceMappingURL=initAction.cjs.map
