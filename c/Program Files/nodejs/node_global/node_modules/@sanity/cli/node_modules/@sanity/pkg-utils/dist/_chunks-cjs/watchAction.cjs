"use strict";
var watch = require("./watch.cjs"), handleError = require("./handleError.cjs");
async function watchAction(options) {
  try {
    await watch.watch({
      cwd: process.cwd(),
      strict: options.strict,
      tsconfig: options.tsconfig
    });
  } catch (err) {
    handleError.handleError(err);
  }
}
exports.watchAction = watchAction;
//# sourceMappingURL=watchAction.cjs.map
